#include <iostream>#include <cstdio>#include <cmath>#include <algorithm>#include <vector>using namespace std;int n, m, tmp;vector<int> vec;int main() {	cin >> n >> m;	for (int i = 0; i < n; i++) {		scanf("%d", &tmp);		vec.push_back(tmp);	}	for (int i = 0; i < m; i++) {		scanf("%d", &tmp);		vec.push_back(tmp);	}	sort(vec.begin(), vec.end());//	vec.erase(unique(vec.begin(), vec.end()), vec.end());	for (int i = 0; i < vec.size(); i++) {		printf("%d ", vec[i]);	}}