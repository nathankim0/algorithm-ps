#include <iostream>using namespace std;int main() {	int n, k,nP=1,kP=1,tP=1;	cin >> n >> k;	for (int i = 0; i < n; i++) { nP *= (n - i); }	for (int i = 0; i < k; i++) { kP *= (k - i); }	for (int i = 0; i < n - k; i++) { tP *= (n - k - i); }	cout << nP / (kP*tP);}