#include <iostream>#include <vector>#include <algorithm>using namespace std;int main() {	int n,m;	scanf("%d", &n);	vector<int> vec(n);	for (int i = 0; i < n; i++) {		scanf("%d ", &vec[i]);	}	scanf("%d", &m);	vector<int>vec2(m);	for (int i = 0; i < m; i++) {		scanf("%d", &vec2[i]);	}	sort(vec.begin(), vec.end());	for (int i = 0; i < m; i++) {		printf("%d ", upper_bound(vec.begin(), vec.end(), vec2[i]) - lower_bound(vec.begin(), vec.end(), vec2[i]));	}	//for (int i = 0; i < m; i++) {	//	cnt.push_back(0);	//	for (int j = 0; j < n; j++) {	//		if (vec2[i] == vec[j]) {	//			cnt[i]++;	//		}	//	}	//}}