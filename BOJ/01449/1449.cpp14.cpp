#include <iostream>#include <algorithm>#include <vector>using namespace std;/*y + 0.5 - (x - 0.5) <= 테이프길이--*/int main() {	int n, l;	cin >> n >> l;	vector<int>vec(n);	for (int i = 0; i < n; i++) {		scanf("%d", &vec[i]);	}	sort(vec.begin(), vec.end());	int cnt = 1;	int t=vec[0];	for (int i = 0; i < n; i++) {		if(vec[i]+0.5-(t-0.5)>l){			t=vec[i];			cnt++;		}	}	cout << cnt;}